/// Swift Package Manager requires a folder with our package's target name to exist inside the Sources folder,
/// and a file has to be inside (unless we state the folder name in the target). This is used because we are distributing
/// the widgets as a binary framework temporarily. When we distribute the files directly, this should be removed.
struct SalemoveSDK { }
